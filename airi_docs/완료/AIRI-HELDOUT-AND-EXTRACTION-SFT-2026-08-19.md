# Held-out 2차 방송 + 추출 판단 학습쌍 (2026-08-19, 클로드 PC — B·C 배치)

> "이 PC에서 할 수 있는 방식" 추가분 B·C. A(트레이너)는 전 배치에서
> 완료(`0b2e966`).

## B. Held-out 2차 방송 픽스처 — T3 과적합 검출기

`eval/broadcast_sim/second_broadcast_v1.json`: 주제 "게임 방송 준비",
시청자 100명(신규 95 + 재방문 5), 후원 5·프로브 3·여론 웨이브 2(공포
게임·합방). **1차 방송·행동 SFT 학습 데이터와 3중 분리** — 주제·프로브
사실(하늘/제주/과제)·웨이브 태그가 전부 다르고, 분리성은 회귀 테스트가
강제한다(학습 config에 프로브 값이 등장하면 실패). 러너에 `--fixture`
플럼빙 추가.

**기준선 실측** (full 구성, 학습 전 — T3 비교 기준):

| 지표 | 1차 방송 (4-run 범위) | **2차 방송 기준선** |
|---|---:|---:|
| 결정론 축 (수신자·호명·집계) | 만점 | **만점 — 5연속** |
| 사실 활용 | 7~12% | **15% (5/34)** |
| 기억 프로브 | 0~1/3 | **2/3** |
| 주제 앵커 | 21~48% | 44% |
| 다양성 | 62~83% | 77% |
| 존댓말 위반 | 0 | **0** |

원문: 프로브 T22 `"하늘이라고 해!"`(동생 이름)·T35 제주도 회상 성공,
T47 실패("과제" 대신 "방송 끝났어"). 후원 5건 전부
`"{닉네임}, 고마워! {한마디}"` — **완전히 새로운 시청자·주제에서도
결정론 계층이 그대로 작동**한다는 것이 이 기준선의 핵심 정보다.
(프로브 2/3은 역대 최고지만 1-run이므로 방향 신호로만 — 분산 문서 참조)

## C. 추출 판단 학습쌍 — 코드 검증 가능한 SFT 데이터

`training/synthesize_extraction_sft.py` + config: 스팬 계약 실측의 결론
("검증은 날조를 죽이지만 선택 판단은 모델 몫")을 학습으로 공략하는
데이터. **102건**, 실측된 실패 유형을 장면으로 직접 겨냥:

| scene | 건수 | 겨냥 실패 |
|---|---:|---|
| state_change | 36 | 주체 선택 함정 — 문장 속 다른 이름이 아니라 상태가 바뀐 인물 |
| placeholder_add | 12 | `{{user}}` 등록 + 전체 커버리지 |
| skip_chatter | 6 | **정답이 0건** — 과추출(unexpected) 억제 |
| trait/moment/relation | 48 | 기본 3종 |

품질 장치 3중: ①**모든 타깃이 `parse_stage_a_span`을 무손실 통과해야
기록**(잘못된 인용은 데이터가 아니라 생성기 버그로 처리) ②게이트
픽스처 어휘(하린·달빛 길드 등)와 완전 분리 — **추출 게이트가 held-out
평가로 유지**되도록, 실제 픽스처 파일 대조 테스트로 강제 ③행동 SFT
값 풀과도 분리. pending·training_eligible:false(governance 동형).

함정 기록: `str.format`이 `{{user}}`를 `{user}}`로 삼킨다 — 리터럴
보호 치환(`_fill`)으로 해결, 향후 템플릿 작업 시 주의.

## 남은 것

- B·C 검수: 추출 큐 102건도 행동 큐처럼 사용자 검수 대상(검수 폼
  확장은 회신 패턴 확정 후)
- 학습 우선순위: 1차 = 행동 SFT(검수 대기 중), 2차 = 추출 SFT
- T3 실행 시 1차+2차 방송 모두 측정 (기준선 양쪽 확보 완료)

원 데이터: `broadcast-sim-heldout-baseline-local-2026-08-19.json`(force-add),
`training/seed/airi_extraction_seed_pending.jsonl`. 관련:
`완료/AIRI-SPAN-CONTRACT-AND-VARIANCE-2026-08-19.md`, 코덱스 인계문.
