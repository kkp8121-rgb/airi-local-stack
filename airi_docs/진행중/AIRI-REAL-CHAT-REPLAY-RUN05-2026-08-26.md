# AIRI 실제 채팅 재생 run 05 결과 — 후보 A+B 적용 (2026-08-26)

> run 04와 같은 입력(치지직 저챗 VOD 채팅 앞 1,500건, 가명은 `--nickname-style korean`)·같은 스택 인자로,
> 후보 A(P5 축자 인용 제거·안전 화면·재생 후원 티어)와 B(의례·감사 턴 history 되먹임 차단)를 적용한
> commit `beb0559`로 재생했다. **사람 채점은 아직 없다.** §2는 Claude 참고 채점이며 판정은 사람 채점(계약 §4)이 한다.
> 산출물 root: `D:\AIRI-Models\airi-human-eval\20260826-replay-05\`(저장소 밖) — `report.json`, `packet.md`,
> `review.jsonl`, `rating-sheet.html`, `ratings-claude-preliminary.json`, `summary-claude-preliminary.{json,md}`,
> `health-before/after.json`, `runner.exit-code.txt`(0).

## 1. 실행 receipt

| 항목 | 값 |
|---|---|
| 픽업 | 99턴 응답 + 입력 스크리닝 차단 1턴; kind: question 89 · reaction 10 · donation 0 (치즈 102건은 소액으로 일반 채팅 처리, 5,000 이상 1건은 픽업되지 않음) |
| 프록시 health | run 04와 동일(계층·guard·계약 true, num_ctx 4096, memory on, stock 모델 digest pinned) |
| 자동 지표 | service_error 0, fallback 2/99, polite 0, invented 0, topic_anchored 45.5%, chars p50 13 / p95 28 |
| A+B 효과(직접 관측) | thank 렌더러 의례 0턴(run 04: 35), 축자 인용 0(35), "고마워." 단답 0(23), "후원 고마워!" 부착 0(후원 픽업 없음) |
| grounding | health의 grounding/재시도 카운터 변화 없음 — 아래 되묻기는 폴백·교정이 아니라 raw 모델 출력(ACT 마커 뒤 본문) |
| 종료 | 스택 stop, owned listener 0, exit 0 |

## 2. Claude 참고 채점 (99턴, 사람 채점 아님)

| 축 | run 04(사람 기준선) | run 05(Claude 참고) | run 04(Claude 참고) |
|---|---:|---:|---:|
| 방송다움 | 1.87 | 1.70 | 1.89 |
| 맥락 유지 | 1.58 | 1.65 | 1.85 |
| 반응 적절성 | 1.83 | 1.81 | 1.99 |
| 말투 규칙 | 3.60 | 4.22 | 4.03 |
| 사실성 | 2.75 | 3.94 | 3.99 |
| critical | 7.1% | **0%** | 3.0% |
| silence_or_filler | 45.5% | **80.8%** | 44.4% |

같은 채점자(Claude) 기준으로 run 04→05: 치명 3%→0%, 말투·사실성 소폭 상승, 방송다움·맥락·반응은 제자리 또는 하락,
무의미 대꾸는 44%→81%로 악화. **A+B는 후원 경로의 결함(치명·재낭독·"고마워." 붕괴)을 없앴지만 방송다움을 올리지
못했다.** 계약 §4(5축 평균 +0.3, 어떤 축도 −0.2 이하 금지)를 사람 채점에서 통과할 가능성은 낮다.

## 3. 드러난 패턴 — 이제는 모델 고유 행동

1. **"음... 뭐가 X인데?" 되묻기 붕괴(37턴, T47 이후 거의 전부)**: 의례가 사라지자 모델이 시청자 말을 되묻는 문형을
   만들고, 그 문형이 history로 되먹여 굳는다. run 04의 "고마워." 붕괴와 같은 메커니즘(자기 출력 되먹임)이며 이번에는
   하드코딩이 아니라 모델 자신의 문형이다. grounding 재시도·폴백 개입 0.
2. **완전 에코 19턴 + 뜻 되묻기 17턴**: 밈·슬랭·게임 고유명사("둥하", "롤체", "얼엑", "ㄱㄴㅇ")를 모르면 그대로 되풀이하거나
   "무슨 뜻이야?"로 되묻는다. 지식 부재 + 짧은 입력에 대한 기본 전략 부재.
3. **정답 모드 5턴**: 문장형 질문·장난에는 바로 답한다(스트레칭, 명치, 죽는거임, 눈, 물어주시나요).
4. 없는 뜻 지어냄 3턴(목이 유연하다=목소리, 롤체=롤플레잉 채팅, 유교걸).

## 4. 판단과 다음 후보

- A+B는 유지한다(치명 0, 후원 경로 정상화). 남은 결함은 **모델이 짧은 밈·슬랭 채팅에 답을 만들지 못하고 자기 문형에 갇히는 것**이며,
  이는 결정론 계층이 아니라 모델·프롬프트·재료 축이다(실태 문서 §4.2 "결정론으로 못 닫는 행동의 실측" 첫 사례).
- 다음 후보(사용자 결정, M6-10):
  - **D. 되먹임 위생 일반화**(결정론, 소규모): AIRI의 되묻기·에코 전용 응답은 history에 넣지 않는다(B의 일반화). 붕괴 고리를 끊지만
    되묻기 자체를 줄이진 않는다.
  - **E. 모델 축 probe**(평가 전용, 운영 변경 아님): 같은 입력을 `qwen3:8b`(이미 Ollama에 존재, 8GB VRAM Q4 적재 가능)로 재생해
    같은 시트로 채점 — 2.3B 한계인지 프롬프트 한계인지 분리한다. 로드맵 P3-M2(모델 체급 A/B)를 실제 데이터로 하는 것.
  - F. 프롬프트 축: 방송 계약에 "모르는 밈·슬랭은 되묻지 말고 받아치기" 규칙 — 하드코딩 계열이라 D·E 결과 뒤.
  - 파인튜닝은 E의 결과가 "모델 체급을 올려도 같다"일 때만 다시 검토한다.
- 사용자 채점: `rating-sheet.html`(run 05) → JSON → `summarize_ratings.py --ratings ratings-human.json`으로 run 04 기준선과 비교.
